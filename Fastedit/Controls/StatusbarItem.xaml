<UserControl
    x:Class="Fastedit.Controls.StatusbarItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fastedit.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <Flyout x:Name="flyout" Opening="Flyout_Opening">
        </Flyout>
    </UserControl.Resources>
    <Button Height="35" MinWidth="80" Foreground="{x:Bind Foreground,Mode=OneWay}" Content="{x:Bind Text,Mode=OneWay}" Click="Button_Click" x:Name="button" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"/>
</UserControl>
